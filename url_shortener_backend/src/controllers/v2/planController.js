const PlanController = require('../base/planController');

class V2PlanController extends PlanController {
    // Add or override methods specific to V2 if needed
}

module.exports = new V2PlanController();
