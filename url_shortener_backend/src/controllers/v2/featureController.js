const FeatureController = require('../base/featureController');

class V2FeatureController extends FeatureController {
    // Add or override methods specific to V2 if needed
}

module.exports = new V2FeatureController();
