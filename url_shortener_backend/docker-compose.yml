version: "3.8"
services:
  backend:
    build: .
    container_name: url_shortener_backend
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    working_dir: /app
    command: npm run dev
    environment:
      NODE_ENV: development
